<article class="widget">
    <header class="widget__header">
        <div class="widget__title">
            {% if weapon_model.image is not null %}
            <img class="media-object" src="{{ vich_uploader_asset(weapon_model.image, 'file') }}" alt="weapon" width="auto"
                 height="100%">
            {% endif %}
        </div>
        <div class="widget__config">
            {% block configuration_button %}
                <a href="{{ showing_route is defined ? showing_route : '#' }}">
                    <i class="fas fa-eye"></i>
                </a>
                <a href="{{ manage_route is defined ? manage_route : '#' }}">
                    <i class="fas fa-wrench"></i>
                </a>
            {% endblock configuration_button %}
        </div>
    </header>

    <div class="widget__content">
        <div class="clearfix"></div>
        <div class="members__container">
            <div class="media message checked">
                <div class="media-body">
                    <h4 class="media-heading message__heading">{{ weapon_model.name }}</h4>
                    <input type="checkbox" class="btn-more-check" id="more2" checked>
                    <div class="message__details">
                        <table>
                            <tbody>
                            <tr>
                                <td>Nom</td>
                                <td>{{ weapon_model.name }}</td>
                            </tr>
                            <tr>
                                <td>Description</td>
                                <td>{{ weapon_model.description }}</td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</article>