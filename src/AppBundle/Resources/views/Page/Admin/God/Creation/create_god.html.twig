{% extends '@Layout/Page/Admin/base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_rest(form) }}
        <input type="submit" value="{{ 'create'|trans({}, 'action') }}">
    {{ form_end(form) }}
{% endblock body %}